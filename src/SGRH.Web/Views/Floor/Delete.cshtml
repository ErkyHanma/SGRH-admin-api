@model SGRH.Web.Models.Hotel.Floor.DisableFloorModel

@{
    ViewData["Title"] = "Disable Floor";
}

<h1>Disable Floor</h1>

<h3>Are you sure you want to disable this floor?</h3>
<div>
    <h4>Floor</h4>
    <hr />

    <form asp-action="Delete">
        <input asp-for="FloorId" type="hidden" />
        <div class="form-group">
            <label asp-for="UpdatedBy" class="control-label">Updated By (UserId)</label>
            <input asp-for="UpdatedBy" class="form-control" />
            <span asp-validation-for="UpdatedBy" class="text-danger"></span>
        </div>
        <div class="form-group">
            <input type="submit" value="Disable" class="btn btn-danger" />
            <a asp-action="Index" class="btn btn-secondary">Cancel</a>
        </div>
    </form>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}